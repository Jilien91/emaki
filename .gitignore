node_modules/
.DS_Store

# LibreOffice writes a lock file next to any open document
.~lock.*#

# Personal working notes, kept out of the repo
Changes/

# The style guide stays local. It is the method, not the product.
MNEMONICS.md

# Local dev-server config, per-machine
.claude/

# Full Anki export of the upstream deck, used only for local comparison.
# It carries audio filenames, images and pitch accent that this project
# deliberately does not ship. Never commit it.
Kaishi*.txt
